
shell=True expands shell vars in command string
	subprocess.call('ls -l $HOME', shell=True)


subprocess.Popen
	returns open process instance

	communicate
		send data to stdin and wait for process to terminate

	stdin.write
		send data to stdin and return.  Process continues to run


